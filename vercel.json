{
  "version": 2,
  "buildCommand": "pnpm install --no-frozen-lockfile && prisma generate && prisma db push && next build",
  "installCommand": "pnpm install --no-frozen-lockfile",
  "framework": "nextjs"
}
